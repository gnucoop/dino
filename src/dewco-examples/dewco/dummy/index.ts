import {NgModule} from '@angular/core';

export {
};

const EXAMPLES = [
] as any[]; // TODO: remove cast after first example has been added

@NgModule({
  imports: [
  ],
  declarations: EXAMPLES,
  exports: EXAMPLES,
})
export class DummyExamplesModule {
}
