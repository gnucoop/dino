The `@dewco/core/forms` module provides the interfaces for Form Schema and Form Data of [Ajf forms](https://rxdb.info), as well as
data managers for both.

The Form Schema Manager exposes a method for generating filters from a Form Schema.
