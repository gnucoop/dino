load(
    "//tools:defaults.bzl",
    "markdown_to_html",
    "ng_module",
)

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "translations",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    deps = [
        "@npm//@ajf/core",
        "@npm//@angular/core",
    ],
)

markdown_to_html(
    name = "overview",
    srcs = ["translations.md"],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
